@{
    ViewData["Title"] = "Главная";
}

<div class="d-flex flex-grow-1">
    <div class="container-fluid d-flex">
        <div class="row flex-grow-1 d-flex align-items-stretch">
            <div class="col-3">
                hi
            </div>
            <div class="text-center border col-7">
                <h1 class="display-4">Welcome</h1>
                <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
            </div>
            <div class="col-2 bg-secondary overflow-auto" style="height: calc(100vh - var(--header-height))">
                @for (int i = 0; i < 1; i++)
                {
                    <div class="card mt-2 p-2 text-center">
                        <h5>Точки наблюдения</h5>
                        <input type="button" class="btn btn-success" value="Импорт (Excel)"/>
                        <input type="button" class="btn btn-outline-primary mt-1" value="Создать новую"/>
                    </div>
                }
            </div>
        </div>
    </div>
</div>